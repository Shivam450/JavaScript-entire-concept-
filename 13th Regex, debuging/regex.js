/*Regex := Regular Expression,
  */
let regex = /shivam/;
let regex2 = new RegExp("shivam");

//// ^ and $ are called meta character
let pattern = /^a...s$/
console.log(pattern.test("alais"))
console.log(pattern.test("a123s"))

patterrn = new RegExp("foo")



text = "find me me";
pattern = /me/;
for(let i =0; i > text[i]; i++){  text.find(pattern)}
console.log(i);

function validate(num) {
    const = /^\(?([0-9]{Z})\?)
}


